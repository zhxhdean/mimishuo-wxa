
<view class="body">
    <view wx:for="{{secretList}}" wx:key="{{item.secretId}}" class="">
      <view class="c858 user">
        <view><image src="{{item.headImageUrl?item.headImageUrl:'../../images/avator.jpg'}}" class="avator"/> <text>{{item.createTime}}</text> </view>
       
      </view>
      <view wx:if="{{item.content}}" class="content">
        {{item.content}}<view></view>
      </view>
      <view  wx:if="{{item.replyContent}}" class="reply">
        <view class="reply-hr"><strong>HR</strong> <text class="c8b8">{{item.replyTime}}</text></view>
        <view>
          {{item.replyContent}}<view class="triangle"></view>
        </view>
        </view>
    </view>
  
  </view>